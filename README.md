## Environment setup
Clone:

    git clone --recursive https://github.com/luj1985/hypnos.git

Install Meteor:

    curl https://install.meteor.com | /bin/sh

Run it:

    cd hypnos
    meteor reset && meteor